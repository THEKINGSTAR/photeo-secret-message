import os
import shutil
import tempfile
import glob
from shutil import copyfile

#---------------------------------------------------------------

print("Path at terminal when executing this file")
print(os.getcwd() + "\n")
